<div class="container">
  <h1>Add a new restaurant</h1>
  <%= simple_form_for @restaurant do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :rating %>
    <%= f.submit "Create the restaurant now!", class: "btn btn-primary" %>
  <% end %>
</div>
